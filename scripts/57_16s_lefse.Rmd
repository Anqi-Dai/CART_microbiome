---
title: "16s lefse"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
```

```{r}
# sort out the genera counts table and also do filtering (need to have all taxa levels)
meta <- read_csv('../data/amplicon/stool/combined_2_meta.csv') %>% 
  filter(center == 'M')
library(vdbR)
connect_database('~/dbConfig.txt')
get_table_from_database('asv_annotation_blast_ag')
cts <- get_counts_subset(meta$sampleid)

cts_ <- cts %>% 
  select(asv_key, sampleid, count) %>% 
  spread(key = 'sampleid', value = 'count', fill = 0) %>% 
  arrange(asv_key) 

annot <- asv_annotation_blast_ag %>% 
  filter(asv_key %in% cts_$asv_key) %>% 
  mutate(ordr =  if_else(ordr == '', str_glue('of_class_{class}'), ordr),
         family =  if_else(family == '', str_glue('of_order_{ordr}'), family),
         genus =  if_else(genus == '', str_glue('of_family_{family}'), genus),
         species =  if_else(species == '', str_glue('of_genus_{genus}'), species)) %>% 
  mutate(taxa_genus = str_glue('k__{kingdom}|p__{phylum}|c__{class}|o__{ordr}|f__{family}|g__{genus}'))

cts_genus_all <- cts_ %>% 
  full_join(annot %>%  select(asv_key, taxa_genus), by  = 'asv_key') %>% 
  select(-asv_key) %>% 
  gather(key = 'sampleid', value = 'count', names(.)[1]:names(.)[ncol(.) - 1]) %>% 
  group_by(sampleid, taxa_genus) %>% 
  summarise(cnt = sum(count)) %>% 
  # get the total count from the db to calculate the relab
  left_join(cts %>% distinct(sampleid,count_total ), by = 'sampleid') %>% 
  mutate(relab = cnt/count_total) %>% 
  select(sampleid, taxa_genus, relab)

# the genera to keep
keepg <- cts_genus_all %>% 
  filter(relab > 0.0001) %>%
  ungroup() %>% 
  count(taxa_genus) %>% 
  filter(n > floor(nrow(meta) * 0.1)) %>% 
  filter(!str_detect(taxa_genus, 'NA$')) %>% 
  pull(taxa_genus)

cts_genus_fil <- cts_genus_all %>% 
  filter(taxa_genus %in% keepg) %>% 
  spread('sampleid', 'relab')
 
```

```{r}
# the pheno label for the samples
pheno <- meta %>% 
  select(center, cr_d100:crs, icans, sampleid) %>% 
  gather('pheno', 'value', cr_d100:icans) 

all_sub_pheno <- pheno %>% 
  split(., list(.$pheno)) %>% 
  purrr::imap(~ filter(.data = ., value != 'not_assessed'))

tpheno <- all_sub_pheno %>% 
  imap(function(.x, .y){
    select(.data = .x, value, center) %>% 
      t() %>% write.table(str_glue('../data/amplicon/lefse/pull_{.y}.txt'), sep = '\t', quote = F, row.names = T, col.names = F)
  })

tcts <- all_sub_pheno %>% 
  map(~ pull(.data = ., sampleid) ) %>% 
  imap(~ cts_genus_fil %>% select(taxa_genus, matches(.x)) %>%  write_tsv(str_glue('../data/amplicon/lefse/{.y}_tcts.tsv'))) 
```

cat pull_toxicity.txt toxicity_tcts.tsv > pull_toxicity_tcts.tsv
cat pull_cr_d100.txt cr_d100_tcts.tsv > pull_cr_d100_tcts.tsv 
cat pull_crs.txt crs_tcts.tsv > pull_crs_tcts.tsv
cat pull_icans.txt icans_tcts.tsv > pull_icans_tcts.tsv

```{r}
fns <- list.files('../data/amplicon/lefse/', pattern = 'pull.*_tcts.tsv$')

cmds <- tibble(
  fns = fns
) %>% 
  mutate(format_cmd = str_glue('format_input.py {fns}  {fns}.in -c 1 -s 2  -u 3  ')) %>% 
  mutate(run_cmd = str_glue('run_lefse.py {fns}.in  {fns}.res')) %>% 
  mutate(plot_cmd = str_glue('plot_res.py {fns}.res {fns}.png  --feature_font_size 4 --width 10 --dpi 300 --title {fns}')) %>% 
  mutate(clado_cmd = str_glue('plot_cladogram.py {fns}.res {fns}_clado.pdf  --label_font_size 4 --dpi 300 --format pdf --title {fns}')) %>% 
  select(-fns) %>% 
  gather() %>% 
  select(value) %>% 
  write_csv('../data/amplicon/lefse/lefse_run_cmd_taxa.sh', col_names = F)
 
# No differentially abundant features found in the 4
# also this is the case when using center as subclass 
```

  