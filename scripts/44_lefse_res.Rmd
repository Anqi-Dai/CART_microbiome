---
title: "the lefse results look"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(tidyverse)
```

```{r}
ret <- tibble(
  fn = list.files('../data/shotgun_output/humann3/', pattern = 'lefse_ready.+res$', full.names = T)
) %>% 
  mutate(grp = if_else(str_detect(fn, 'pcts'), 'pathway', 'KO'))
```

